{{ form_start(Arcform) }}
    {{ form_row(Arcform.nom_a) }}
    {{ form_row(Arcform.nbr_chap) }}
    {{ form_row(Arcform.nbr_anime) }}
    {{ form_row(Arcform.desription) }}
    <button type="submit" class="btn btn-primary"> {{ button_label|default('Ajouter') }} </button>
{{ form_end(Arcform) }}